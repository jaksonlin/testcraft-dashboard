# Database Configuration
POSTGRES_DB=test_analytics_v2
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Nginx Configuration
NGINX_PORT=80

# Spring Boot Configuration
SPRING_PROFILES=prod

# Java Configuration
JAVA_OPTS=-Xmx512m -Xms256m

# Optional: Backend port (uncomment in docker-compose.yml to expose)
# BACKEND_PORT=8090

# Optional: Frontend port (uncomment in docker-compose.yml to expose)
# FRONTEND_PORT=5173

